# 十二生肖资源迁移来源登记

## 当前正式入口

- 角色类与表现资源：`/Game/DBA/Data/Registries/DA_DBA_ZodiacCharacterRegistry`。
- 选角与创建角色说明、属性倾向与技能说明：`/Game/DBA/Data/Registries/DA_DBA_ZodiacCharacterSelection`。
- 正式网格：`/Game/DBA/Zodiacs/Chinese/Visuals/Meshes/SKM_DBA_Zodiac_*`。
- 正式材质：`/Game/DBA/Zodiacs/Chinese/Visuals/Materials/Instances/MI_DBA_Zodiac_*`。
- 正式 AnimBP：`/Game/DBA/Zodiacs/Chinese/Animations/ABP_DBA_Zodiac_*`。

`DA_DBA_ZodiacCharacterRegistry` 已包含十二生肖的角色类、网格、AnimBP 和材质软引用。玩家选角、创建角色和大厅角色不得再按 C++ 路径、Rosales、引擎模板或旧目录进行候选回退。

## 迁移来源，仅保留不运行

| 来源目录 | 保留内容 | 状态 | 清理前置条件 |
| --- | --- | --- | --- |
| `/Game/Models/Zodiac` | 12 个旧 `SK_*_Mesh` | 迁移来源，不允许新增运行时引用 | Asset Registry 引用审查为零。 |
| `/Game/Animation/Zodiac` | 12 个原始 `ABP_*` 和蒙太奇 | 迁移来源，原始 AnimBP 已复制到正式目录 | 新 AnimBP 的骨架、动画与人工展示审核通过。 |

## 本次迁移记录

1. 已复制 12 个旧 AnimBP 到正式目录，命名为 `ABP_DBA_Zodiac_<生肖>`。
2. 旧 AnimBP 未移动、未删除，便于人工比较与回滚。
3. C++ 已移除对 `/Game/Models/Zodiac`、`/Game/Animation/Zodiac` 的运行时候选路径；资源选择改由注册表数据资产解析。
4. `bUseTintedPlaceholderMesh` 已在项目配置中关闭，Rosales 不再作为玩家展示回退。

## 后续清理门槛

1. 官方 Unreal MCP 的 Asset Registry 引用查询恢复可用。
2. 逐项确认旧网格、旧 AnimBP 和旧蒙太奇没有地图、蓝图、数据资产或打包规则引用。
3. 对十二生肖逐项进行人工选角、创建角色、大厅展示审核，确认网格、材质和动画正确。
4. 仅在以上三项通过后，按单生肖小批次移动或删除迁移来源资产；不得批量删除。
